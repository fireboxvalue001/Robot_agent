# 委托单文档样例

本目录保存从“意图识别模块_本地测试包”复制的委托单 PDF，原测试包未被修改。

- `templates/`：空白检测校准委托单模板。
- `samples/`：6 份用于意图识别和能力边界验证的委托单样例。

这些文件仅用于开发和测试，不代表对应检测项目均可由 VD10 执行。上传后仍应以文档意图接口返回的 `capability_assessment` 和 `clarification` 为准。
